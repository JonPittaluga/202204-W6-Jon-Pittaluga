import { getNeighbours } from './get-neighbours.js';

describe('', () => {
  describe('', () => {
    test('Shoud capture the error', () => {
      // expect(() => {}).toThrow(TypeError);
    });
  });
});

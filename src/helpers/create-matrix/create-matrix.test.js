import { createMatrix } from './create-matrix.js';

describe('', () => {
  describe('', () => {
    test('Shoud capture the error', () => {
      // expect(() => {
      //   createMatrix(); // Both null and undefined work as inputs;
      // }).toThrow(TypeError);
    });
  });
});
